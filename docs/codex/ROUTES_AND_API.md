# Routes and API

## Frontend route groups

Routing is a small History API implementation in `app/navigation/routes.ts`; React Router is not used. `appRouteFromPathname()` maps paths to a main view, overlay or mobile workflow. Desktop main navigation and mobile workflows intentionally differ.

| Group | Paths / route state | Screen/owner | Access |
| --- | --- | --- | --- |
| Authentication shell | Any application route without an active session | `AuthScreens.tsx`, `useBookMeetController.tsx` | unauthenticated visitors see only login/registration; the requested same-origin path is restored after login |
| Main authenticated views | `/liked`, `/saved`, `/notifications`, `/chat`, plus all main paths | `ContentScreens.tsx`, `ChatScreen.tsx`, `app/components/notifications/Notifications.tsx` | session; content remains viewer-filtered |
| Profile tabs | `/profile`, `/profile/books`, `/profile/blog`, `/profile/news`, `/profile/library`, `/profile/wishlist`, `/profile/communities`, `/profile/reviews`, `/profile/events`, `/profile/occasions`, `/profile/friends`, `/profile/admin` | `ProfileScreens.tsx` | own profile, public profile or friendship/privacy; admin tab admin |
| Dynamic overlays | `/users/:id`, `/books/:id`, `/shelves/:id`, `/events/:id`, `/reviews/:id`, `/blog/:id`, `/meet/:id`, `/publishing/:id`, legacy direct `/chat/:id`, flagged group `/chat/groups/:id`, `/notifications/:id`, `/reports/:kind/:id` | routed popup logic and controller open/close helpers | group overlay is absent unless bootstrap projects the closed-default feature; the legacy direct route keeps its contract |
| Mobile-only workflows | `/search`, `/create|edit/{review,publication,event,occasion,book,news,reading-goal,shelf}[/:id]`, `/edit/book-status/:id`, `/profile/{followers,following,friends,incoming,outgoing}` | `MobileGlobalSearchPage.tsx`, mobile forms and `routes.ts` | mobile media query; mutation requires session/ownership |

`openOverlayRoute` and `closeOverlayRoute` own History API stack/back behavior. Direct SPA paths are served by `server/index.js` fallback in production.

## Backend endpoint groups

All paths below are mounted under `/api` (for example, `/api/auth/login`, `/api/bootstrap/session`, `/api/books`, `/api/events`, `/api/occasions`, `/api/social/messages`, `/api/admin/reports/:id` and `/api/realtime`). `apiFetch` sends same-origin credentials and locale headers. `server/api.js` handlers apply `authenticatedUser`, `isAdmin`, ownership, block, age and legal checks as appropriate; a UI-hidden action is not an authorization boundary.

| Group | Representative endpoints | Auth requirement | Handler/module/data |
| --- | --- | --- | --- |
| Health/public/auth | `GET /health`, `/auth/providers`, `/auth/legal-documents`, `/public/catalog`; `/auth/login`, `/auth/register`, Google callback/credential, email verify/reset, logout, deleted restore/new | the application UI remains authentication-only without a session; auth bootstrap is private, while narrowly projected support endpoints remain data-minimized | `server/api.js`, `server/security.js`, `server/modules/compliance.js`, `server/modules/public-catalog.js` |
| Bootstrap/location | `GET /bootstrap`, `/bootstrap/:section`, `/cities` | session bootstrap; public city/catalog branches as implemented | `server/modules/bootstrap-router.js`, `location-router.js`, `server/data.js` |
| Search | `GET /search/materials` | authenticated viewer for private-aware search | `server/modules/material-search.js`, `server/data.js` |
| Account/profile | `PUT /users/me/state`, `PATCH /users/me/home-view`, `/users/me/profile-complete`, `DELETE /users/me/profile`, `DELETE /users/me/avatar`; `POST /users/me/telegram-link`, `POST /users/me/telegram/test`, compatibility `GET/PATCH /users/me/telegram-notifications`, `DELETE /users/me/telegram`; linked profile create/attach/google/switch/delete | current user; Telegram link creation requires explicit runtime readiness and returns a 15-minute deep link. Disconnect clears the delivery binding, pending link and normalized Telegram preferences but retains the independent Telegram auth subject. Destructive delete/merge has explicit session checks. | `server/api.js`, `server/modules/account-tokens.js`, `notification-preferences.js`, `notification-channels.js`, `compliance.js` |
| TOTP | `GET /auth/totp/status`, `POST /auth/totp/setup/start|confirm`, recovery regenerate, disable | current user; setup/disable verifies password/TOTP state | `server/security.js`, `server/api.js` |
| Books/library | `GET /books/catalog?q=` (trimmed, normalized catalogue; empty `q` returns the full permitted catalogue, non-empty `q` is bounded to 100; canonical books survive a creator account soft-delete), `/books`; `POST /books`, `PATCH`/`DELETE /books/:id`; `POST /books/preview`; admin import preview/resolve | catalog viewer; writes owner/session; status writes are transactionally validated for five reading states and return the owner book DTO plus completion history. Other viewers receive only status, computed percentage and completion indicator. `DELETE /books/:id` removes only the caller's relation and unfinished cycles; import admin | `server/api.js`, `server/data.js`, `material-input.js`, image storage |
| Reading goals/statistics | `GET/POST /reading-goals`, `PATCH`/`DELETE /reading-goals/:id`, `GET /reading-statistics?year=&month=` | Current user only; goals are deliberately excluded from bootstrap and all other-user projections. The statistics endpoint calculates pace and annual allocations server-side using the request timezone. When the reading-session flag is on it additionally returns deduplicated `bookCounts`/`bookMonths`, exact-second `timeCounts`/`timeMonths`, and `currentReading` with cycle time and progress. Running time is capped at the lease and split by its local dates; legacy `counts` remains for existing clients. | `server/api.js`, `reading-goals.js`, `reading-statistics.js`, `reading_cycles`, `reading_session_days` |
| Flagged reading-session foundation | `GET /reading-sessions/active`; `POST /books/:bookId/reading-sessions/timer/start`; `POST /reading-sessions/:id/pause|resume|heartbeat|stop`; `GET/POST /books/:bookId/reading-sessions`; `PATCH/DELETE /reading-sessions/:id` | Closed unless `BOOK_MEET_READING_SESSIONS_ENABLED=1|true`. Owner-only records and mutations, requiring a personal library relation for creation; one active timer across all books. A new session links the active reading cycle, or the latest completed cycle for a finished book. Start and stop are idempotent. A 120-second lease caps elapsed time after interrupted heartbeats; expired resume requires `confirmExpired: true`. Manual entries require exact local date and 1–86400 seconds split into numeric hours/minutes/seconds. Timer day rows split at midnight in its stored timezone. Mutations emit an owner-only content-free `reading` SSE invalidator; heartbeat never emits a global refresh. | `054`, `server/modules/reading-sessions.js` |
| Flagged reading presence | `GET/PATCH /reading-presence/preferences`, `GET /books/:bookId/reading-presence`, `event: reading-presence` on `/realtime` | Closed behind the same reading-session flag. Audience defaults to `nobody`; setting accepts exactly `nobody/friends/followers/everyone`. Book presence requires the viewer's personal library relation and filters current lease, block, age and adult-book rules before returning only user ID/initials/color/avatar. Timer and setting mutations signal only presently authorized connected viewers (including the former audience on a privacy reduction); no profile/book identity is embedded in the SSE event. | `055`, `server/modules/reading-presence*.js` |
| Flagged marketplace listing slice (partial Stage 6C) | `GET /marketplace/listings`, `GET /marketplace/listings/mine`, `GET /marketplace/listings/:id`, `POST /marketplace/listings`, `PATCH/DELETE /marketplace/listings/:id`, `GET /marketplace/images/:imageId` | Closed unless `BOOK_MEET_MARKETPLACE_ENABLED=1|true`. Personal profiles with a known age of at least 18 only. Public feed returns newest active listings with bounded text/city/type/condition/price filters, block and current seller-age/profile filtering, `limit` (max 40), `beforeId` cursor and nullable `nextBeforeId`; owner may read non-removed listings and edit, change status or soft-remove them. Sale/exchange fields, catalogue/manual book choice, city and 0–6 image data URLs are server validated. Images are signature/size-checked, kept outside public uploads and served only through a current-age/status/block-checked no-store API; prior public marketplace upload paths are denied. Creates are rate-limited and reject a recent same-seller duplicate. | `056`, `server/modules/marketplace-listings.js`, `image-storage.js` |
| Flagged marketplace dialogue slice (partial Stage 6C) | `GET /marketplace/conversations`, `POST /marketplace/listings/:listingId/conversations`, `GET /marketplace/conversations/:conversationId`, `GET/POST /marketplace/conversations/:conversationId/messages`, `DELETE /marketplace/conversations/:conversationId` | Adult personal profiles only, checked for both current participants on list, detail and message access. A buyer can open exactly one conversation per listing without friendship. Seller/buyer membership and two-way block rules are server-enforced. Up to three buyer messages before the seller's first response, including concurrent sends. Hiding a conversation affects only the current user and a later message restores visibility. Removed/closed listings have no new conversations; unanswered buyer threads disappear and answered threads show a neutral card. Realtime sends ID-only signals. The client conversation UI and mocked-route desktop/mobile browser checks exist; real-API visual acceptance and legal confirmation remain before release. | `056`, `server/modules/marketplace-conversations.js`, `app/components/marketplace/MarketplaceConversations.tsx` |
| Marketplace reports/moderation (partial Stage 6C) | `POST /reports` accepts `marketplace_listing` and `marketplace_conversation`; `PATCH /admin/marketplace/listings/:listingId/moderation`; `GET/POST/DELETE /admin/marketplace/sellers/:sellerId/restriction` | An adult may report a visible listing or a conversation in which they are a member. An admin may soft-remove an offending listing and apply or lift a new-listing restriction with a mandatory reason; each action is audited. The admin restriction read returns only the current active restriction. The restriction is checked before listing image writes and in the create transaction. Admin complaint UI shows retained marketplace titles/dialogue messages and uses the dedicated moderation route for listing removal; real-API visual acceptance and legal confirmation remain before release. | `057`, `server/modules/marketplace-moderation.js`, `server/api.js`, `server/data.js`, `app/components/safety/AdminSafety.tsx` |
| Progress notes | `GET/POST /books/:id/notes`, `PATCH`/`DELETE /book-notes/:id` | authenticated viewer for spoiler-filtered reads; owner for writes. Creation requires a currently reading personal library relation and uses the locked server progress snapshot. | `server/api.js`, `book-progress-notes.js`, `book_progress_notes` |
| Book shelves | `GET /users/:id/shelves`, `GET /shelves/:id`, `POST /shelves`, `PATCH`/`DELETE /shelves/:id`, `POST /shelves/:id/add-to-library` | authenticated viewer with block/hide/age filtering; reader owner for CRUD. Batch add inserts only missing readable books as `want`. | `server/api.js`, `book-shelves.js`, `book_shelves`, `book_shelf_items` |
| Community books | `POST /community-books`, `PATCH`/`DELETE /community-books/:id` | only the owning community may associate an existing canonical book; no canonical book mutation or reader status fields | `server/api.js`, `user_books.featured_*` |
| Materials | `POST/PATCH /reviews`, `POST/PATCH /excerpts`, `DELETE /materials/:kind/:id`; admin patch/delete `/admin/materials/:kind/:id` | owner routes create or edit only the caller's material; admin endpoints are separate moderation paths | `server/api.js`, `content-security.js`, `compliance.js` |
| Wishlist | `/wishlist/preview`, `POST /wishlist`, `DELETE /wishlist/:id`, price, reserve/unreserve | session; owner and friendship/privacy checks for gift reservation | `server/api.js`, external book source helpers |
| Feed interactions | `GET/POST/DELETE /reactions`, `/saves`, `/comments`, `/comments/:id/like`; `GET /material-stats`; `POST /materials/:type/:id/repost`, `DELETE /reposts/:id` | session for writes; readable material, block, age and one-way hide checks for reads. Comment replies stay under their root; tombstones retain visible descendants. | `server/api.js` and request-local material permission cache |
| One-way hide and mentions | `POST/DELETE /users/:id/hide`, `GET /users/me/hidden-users?cursor=`, `GET /users/mentions?q=` | hiding preserves profiles/relationships/messages/notifications but removes hidden authors' social content from viewer-aware material reads; mention links use stored IDs and public notification dedupe keys. | `server/api.js`, `server/data.js`, `user_hides`, `content_mentions` |
| Events/occasions | `POST/PATCH/DELETE /events`, attendee list, reminder; admin event moderation; matching `/occasions` and admin moderation | session; creator ownership; admin moderation | `server/api.js`, `material-input.js`, `server/data.js` |
| Social | friend request create/delete, accept/reject/remove; follow/unfollow; block/unblock | session; pair policy, age, profile type and block checks | `server/modules/social-permissions.js`, `compliance.js`, `server/api.js` |
| Chat/notifications/realtime | message CRUD/reactions/read/history; authenticated `GET /stickers`; `PATCH /messages/:id`, `DELETE /messages/:id`, POST/DELETE `/messages/:id/reactions/like`; `GET /conversations/:id/messages/search?q=&cursor=&limit=` and grouped `GET /messages/search?q=&cursor=&limit=`; notification APIs; authenticated `GET /realtime` SSE | Session plus current friends/community/**publisher**/admin pair, block and age rules. An approved `publisher_news` is a readable share attachment for both participants. Emoji is ordinary normalized text. A sent sticker accepts only an enabled stable Book Meet catalog id and has no text, mentions or attachment; an archived catalog definition resolves existing sticker DTOs. Search accepts at most 8 tokens/100 characters, requires at least 3 characters per token, uses boolean prefix matching through `messages_body_fulltext`, and has no Russian stemming. It excludes deleted messages, per-viewer cleared history, sticker content and all private evidence. Conversation denial is a non-disclosing `404`; grouped results silently omit inaccessible peers. Results contain id, plain-text snippet, date and author identity, use bounded cursor pages, and are rate-limited. After a successful committed chat mutation, SSE emits a content-free `chat` invalidation only to the affected participants (history clear: current viewer only); error responses emit no event. The client ignores event data and reloads `/bootstrap/social`. | `server/api.js`, `server/modules/book-stickers.js`, `server/modules/message-search.js`, `messages`, `chat_history_clears`, notification tables, SSE broadcaster |
| Flagged group-chat foundation and UI slices 1–2 | `GET/POST /group-conversations`, `GET /group-conversations/candidates?q=`, `GET/PATCH/DELETE /group-conversations/:conversationId`, member add/remove/role/owner-transfer/leave, history-clear/read; explicit group message send/list/edit/delete/reaction/search and poll create/list/vote routes. Client reads only the server-projected feature and opens `/chat/groups/:conversationId`. | Closed unless `BOOK_MEET_GROUP_CHATS_ENABLED=1|true`; this explicit namespace avoids the legacy `/conversations/:peerId` direct-search contract. A non-advancing read cursor is a no-op (no timestamp/SSE write). The list has group-only last-visible-message/member/unread projection and hides groups after a block or cross-age change between the viewer and an active member; ordinary activity invalidation also excludes those members. `GET .../messages` returns the newest 200 in chronological order with `nextCursor`; `?before=<messageId>` pages older messages without crossing the global clear cursor. Group avatar create/update accepts image data URLs, while arbitrary upload paths are rejected. Candidate search projects only contacts allowed by the direct-message permission model. Active membership is required for ordinary reads; policy-controlled additions recheck direct-contact permission for inviter and block/age compatibility against every active member. Owners alone update policy/delete/transfer ownership; owner/moderator can clear history and another member's message. Group sends apply the existing sticker, attachment and structured-mention restrictions across every active member; group search ignores the per-member read cursor but applies the global clear cursor and excludes deleted/system/sticker/evidence rows. Each poll is bound to a system message, so global clear hides its card. Voting locks the poll, validates the option’s same-poll key, and projects aggregate counts/viewer selection only. Owners/moderators leave closed role-specific tombstones. Community creation requires a session whose auditable operator is the active linked personal profile; publisher creation is self-identity only. | `053`, `group-chat-feature.js`, `server/api.js`, `useGroupChats.ts`, `GroupChatComponents.tsx`, `tests/browser/group-chats.spec.ts`. Slice 2 includes desktop/mobile role settings, lifecycle confirmations, search, emoji/stickers and poll create/vote controls; the browser acceptance runs in both viewport projects. |
| External notification callbacks | `POST /integrations/telegram/webhook`; `GET/POST /notifications/email/unsubscribe` | public but cryptographically authorized: Telegram requires its secret header plus a valid unused/ unexpired deep-link token; email requires a valid signed, expiring token and confirmation POST. Neither endpoint exposes notification content or account data. | `server/api.js`, `server/modules/notification-channels.js`, `telegram_link_tokens`, notification preferences/deliveries |
| Reports/moderation | `POST /reports`, mine, appeal; admin report status/processed/delete-material; admin user suspension/restore/permanent | authenticated reporter; admin for review/destructive actions | `server/api.js`, `compliance.js`, `moderation_audit_log` |
| Legal/admin/compliance | `/legal/acceptances`; admin legal-document CRUD, audit/security logs, sessions, age audit, incidents, statistics; `GET /admin/notification-deliveries/statistics` | legal acceptance user; admin for document/audit/statistics writes/reads. Notification delivery statistics expose only aggregate channel/status/due counts, never recipient or content. | `server/modules/compliance.js`, `server/api.js`, `AdminCompliancePanel.tsx`, `AdminStatisticsPanel.tsx` |

## Compatibility/demo notes

Reading-goal creation validates positive integer counts and calendar periods in `X-BookMeet-Timezone`: months from the current month through December, next year always, current year only in January/February. Annual `NULL` months use a generated unique slot, so duplicate concurrent requests return `409 READING_GOAL_DUPLICATE`. `PATCH /reading-goals/:id` changes the count; period changes return `422 READING_GOAL_PERIOD_IMMUTABLE`. An unknown or another user's goal returns `404`; administrator status does not grant access to other users' private goals. Statistics use completed `reading_cycles`, including rereads and history retained after a library relation is removed.

`server/demo-api.js` implements an in-memory subset for demo smoke and is not the MySQL production contract. `/api/bootstrap` remains for older clients while new client code requests sections. Endpoint details are intentionally grouped here; inspect `server/api.js` at the route group before changing a handler.

The legacy Telegram notification GET/PATCH routes project and update the normalized category preferences; `telegram_subject` alone never makes the delivery channel connected or available. Legacy category names are a lossy compatibility view over the canonical categories. `DELETE /users/me/telegram` disables every normalized Telegram category, invalidates a pending deep link and removes the verified delivery ID/timestamp/display label while retaining the auth subject used by account identity flows.

For an exact generated production route inventory, see [generated/API_ROUTES.md](./generated/API_ROUTES.md) and [generated/FRONTEND_ROUTES.md](./generated/FRONTEND_ROUTES.md). They are refreshed with `pnpm docs:generate` and checked for drift by `pnpm docs:check`; this document remains the semantic ownership and access guide.

## Progress note contracts

`GET /api/books/:id/notes?scope=mine|all&cursor=` returns `{notes,nextCursor}` in descending ID order with server-side visibility filtering before pagination. `POST /api/books/:id/notes` accepts `{body,expectedProgress?:{unit,current,total,percent}}` for a currently reading personal library book. A stale confirmation returns `409 BOOK_NOTE_PROGRESS_CHANGED`. `PATCH /api/book-notes/:id` accepts only `{body}`; `DELETE /api/book-notes/:id` is owner scoped. `POST /api/reports` accepts target kind `book_note` only when the reporter can read that exact note; admin report deletion uses the existing moderation/audit flow.

The book card notes tab is inline. Saving from the personal thoughts field opens a confirmation and keeps the thoughts unchanged. Closed note bodies are never placed in ordinary bootstrap data. Successful writes use the existing response-after-commit realtime broadcast and do not replace the client user snapshot.

## Book shelf contracts

Shelf list responses are cursor-paginated and contain at most the first four book previews per card; `/api/shelves/:id` returns the ordered detail. Title and descriptions use Unicode code-point limits, items must be unique, and creation/editing accepts at least one book from the owner's current library. `/api/shelves/:id/add-to-library` is idempotent and returns added, already-present and unavailable counts plus the added owner DTOs.

`shelf` participates in reactions, comments, private saves, reports, moderation, material search, notifications and chat attachments. Direct shelf routes use `/shelves/:id`; mobile create/edit routes use `/create/shelf` and `/edit/shelf/:id`. Overlay and workflow history retain the full background path including the library `mode=shelves` query.
