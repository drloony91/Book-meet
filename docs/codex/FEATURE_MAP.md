# Feature map

Каждая строка фиксирует сквозной contract: **frontend → API → data/entities → permissions → tests**. Детали таблиц и FK — [DATA_MODEL.md](./DATA_MODEL.md); endpoint-матрица — [ROUTES_AND_API.md](./ROUTES_AND_API.md).

| Feature | Frontend → API | Data/entities → permissions | Tests / contracts |
| --- | --- | --- | --- |
| Auth/account | `app/screens/AuthScreens.tsx`, controller `login/register/logout/resolveDeletedProfile` → `/api/auth/*`, `/api/users/me/profile`, `/api/linked-profiles/*`, `/api/auth/totp/*` | `users`, `sessions`, `account_action_tokens`, `linked_profiles`; `server/security.js`, legal gate, session owner/admin rules | `tests/security.test.mjs`, `tests/compliance.test.mjs`, `tests/production-contract.test.mjs` |
| Profiles, linked profiles, privacy | `ProfileScreens.tsx`, `UsersDirectoryScreen.tsx`, controller `handleUserChange` → `/api/users/me/state`, `/api/users/me/home-view`, `/api/linked-profiles/*` | `profiles` 1:1 `users`, `linked_profiles`; `profileAccessState`, age/birth-date and viewer-aware follower/friend/wishlist visibility. Owner/admin always see, `friends` requires confirmed friendship, never membership; unavailable list data/counts are omitted by `loadUsers`. | `tests/architecture-security.test.mjs`, `tests/desktop-identity.test.mjs`, `tests/mobile-profile-contract.test.mjs` |
| Feed/materials/reactions/saves/comments | `ContentScreens.tsx`, `ContentComponents.tsx`, `useBookMeetController.toggleLike/toggleSave/addComment` → `/api/reactions`, `/api/saves`, `/api/comments`, `/api/material-stats`, `/api/materials/:kind/:id` | reviews/excerpts/publisher_news/events/occasions + polymorphic `material_likes`, `material_saves`, `material_comments`, `material_books`; readable material, block, age, ownership/admin | `tests/security.test.mjs`, `tests/architecture-security.test.mjs`, `tests/mobile-content-routes-contract.test.mjs`, `tests/spreadsheet-lazy-contract.test.mjs` |
| Social content interactions | shared comment controls, profile/material hide actions, mention autocomplete and repost composer → `/users/:id/hide`, `/users/mentions`, cursor comments/likes and repost routes | `user_hides`, rooted/tombstoned comments, comment likes, stable `content_mentions`, normalized `reposts`; all content projections are viewer-aware while chat and notification history are preserved | queue-4 HTTP/UI and MySQL regression coverage |
| Books/catalog/library/wishlist/import | `ContentComponents.tsx`, `components/books/ReadingStateFields.tsx`, `services/library-mutations.ts`, `ProfileScreens.tsx`, `ContentScreens.tsx`, `services/spreadsheet.ts` → `/api/books`, `/api/books/catalog`, `/api/books/preview`, `/api/community-books`, `/api/wishlist/*`, `/api/admin/books/import/*` | `books`, `book_links`, `user_books`, `reading_cycles`, `wishlist_items`; personal state is one of want/reading/read/abandoned/postponed. Owner receives raw pairs/comment and completed history; other viewers only receive projected status/progress. Community associations only reference existing catalogue books; owner, friend reservation, adult material, admin import; external preview validated server-side | `tests/book-query.test.mjs`, `tests/reading-client.test.mjs`, `tests/reading-demo-http.test.mjs`, `tests/profile-completion-community-contract.test.mjs`, `tests/material-search.test.mjs`, `tests/security.test.mjs`, `tests/spreadsheet-lazy-contract.test.mjs` |
| Events | `ContentScreens.tsx`, controller `createEvent/edit/moderateEvent` → `/api/events`, `/api/events/:id/attendees`, `/api/events/:id/reminder`, `/api/admin/events/:id` | `events`, `event_reminders`, `material_books`, optional `books/cities`; creator edits/deletes own pending material, admin moderates, age/city validation | `tests/bootstrap-router.test.mjs`, `tests/compliance.test.mjs`, `tests/production-contract.test.mjs` |
| Occasions | `ContentScreens.tsx`, controller `createOccasion/edit/moderateOccasion` → `/api/occasions`, `/api/admin/occasions/:id` | `occasions`, optional `cities/books/material_books`; creator ownership, admin moderation, age and target profile/city validation | `tests/compliance.test.mjs`, `tests/mobile-content-routes-contract.test.mjs` |
| Social: friendships/follows/communities | `ProfileScreens.tsx`, `UsersDirectoryScreen.tsx`, controller `send/accept/reject/removeFriend`, `followUser` → `/api/social/friend-requests/*`, `/api/social/friends/*`, `/api/social/follows/*` | `friend_requests`, symmetric `friendships`, `follows`, `community_memberships`; publishers use `canMessagePair` alongside `canCreateFriendRequest`, blocks and cross-age cleanup; membership ≠ friendship | `tests/architecture-security.test.mjs`, `tests/compliance.test.mjs`, `tests/desktop-identity.test.mjs` |
| Chat/notifications/realtime | `ChatScreen.tsx`, `ChatComponents.tsx`, `MobileMessagesPage.tsx`, notifications UI and controller chat/search workflows → message CRUD/reactions/read/history, conversation/global message search, authenticated sticker catalog, notification APIs and SSE | `messages` with `FULLTEXT(body)`, reactions, private edit/deletion evidence, per-viewer history clears and notification tables. Text emoji remains ordinary normalized message text; Book Meet-owned stickers are a separate `message_kind=sticker` with a stable `sticker_id`, server-resolved local SVG DTO and evidence retention. Search is rate-limited, authorizes current dialog access without write locks, excludes cleared/deleted content, sticker content and evidence, and returns cursor-bounded safe snippets; global results are grouped by permitted peer and newest match. Three-character boolean prefixes are supported without Russian stemming. Author deletion retains the stage-4 tombstone/evidence rules and mutation lock order. Successful chat mutations emit content-free SSE invalidations only to affected participants; clients reload the server-authoritative social projection and do not trust event payloads. Notification preferences, event/outbox dedupe, cancellation and worker semantics remain server-authoritative. Runtime Telegram/SMTP adapters send only generic projections when their explicit readiness flags are enabled; Telegram binding uses a one-time deep link and email provides a signed unsubscribe action. | `tests/chat-contract.test.mjs`, `tests/message-realtime-demo-http.test.mjs`, `tests/message-search*.mjs`, `tests/notification-*.mjs`, `tests/mysql-integration.test.mjs`, `tests/browser/book-meet.spec.ts` |
| Flagged group lifecycle and UI slices 1–2 | bootstrap `features.groupChats` → `useGroupChats.ts`, `GroupChatComponents.tsx`, `FriendsPanel`/`MobileMessagesPage`, distinct `/chat/groups/:id` → `/group-conversations/*` only while `BOOK_MEET_GROUP_CHATS_ENABLED=1|true` | shared conversations/memberships use active role/policy checks, candidate-vs-every-member contact/block/age checks, per-member read boundaries and global history clear. Current block and age incompatibility hide list/history and suppress activity invalidators. The hook aborts stale requests, de-duplicates read cursors, and never calls group API when the projection is absent. The UI has avatar create/change/clear, authorized contact search, paged history with older-search navigation, role/policy settings, lifecycle controls, emoji/stickers and poll creation/voting; direct legacy routes retain their namespace. | `tests/group-conversations-migration.test.mjs`, `tests/group-conversations-http-mysql.mjs`, `tests/group-chat-ui-contract.test.mjs`, `tests/browser/group-chats.spec.ts`; the flag remains closed by default pending a separate release decision. |
| Flagged reading sessions, statistics and presence | bootstrap `features.readingSessions` → owner session endpoints, `GET /reading-statistics`, separate presence preferences/book presence endpoints and targeted SSE; `ReadingSessions` in the personal book card, `ReadingPresencePreference` in profile settings, `ReadingPresenceAvatars` in focused reading mode, and the segmented `ReadingStatsModal` | `reading_sessions`/`reading_session_days` keep one active lease-bound timer and exact seconds split by local day. Server statistics count each currently completed book once, sum cycle seconds, and group day seconds by month. `profiles.reading_presence_visibility` defaults to nobody; presence is limited to an active lease, own readable library book, selected audience, current block/age/adult rules, and minimal non-clickable avatar identity. The demo adapter has a resettable flagged session, presence and statistics projection; production MySQL remains authoritative. | `tests/reading-sessions.test.mjs`, `tests/reading-statistics.test.mjs`, `tests/reading-presence.test.mjs`, authenticated `tests/reading-*-http-mysql.mjs`, `tests/reading-demo-http.test.mjs`, `tests/browser/reading-sessions.spec.ts`; flags remain closed by default. |
| Marketplace, partial Stage 6C | closed-default `BOOK_MEET_MARKETPLACE_ENABLED` bootstrap projection; listing CRUD/filter API and listing-bound conversation API for known-adult personal users. The `/marketplace` route and desktop/mobile navigation are shown only for eligible adults; the listing and conversation UI supports browse/create/edit, warnings, messaging, hiding and reports. Admin complaint UI exposes listing/conversation evidence, listing removal and seller restriction/lift. Legal release gate and real-API visual acceptance remain. | Listing snapshots and private images validate catalogue/manual book, city, terms and ownership; feed excludes blocked sellers. Unique `(listing, buyer)` conversation metadata prevents duplicates. Message writes serialize on the conversation row, cap buyer messages at three until the first seller reply, honor blocks and hide/unhide per member; removed/closed listings suppress unanswered buyer threads and use neutral cards after a reply. Reports on listing/conversation are membership/age checked. Admins may remove a listing with an audited reason and restrict new listings for a seller. | `056`, `057`, `server/modules/marketplace-listings.js`, `marketplace-conversations.js`, `marketplace-moderation.js`, `app/components/marketplace/MarketplaceListings.tsx`, `MarketplaceConversations.tsx`, `app/components/safety/AdminSafety.tsx`, disposable-MySQL Stage C and desktop/mobile mocked-route browser tests; not ready for release. |
| Search | `MobileGlobalSearchPage.tsx`, controller search workflow → `/api/search/materials`, `/api/cities` | material search currently scopes/filter in bootstrap via `server/modules/material-search.js`; city directory via `cities/city_aliases`; auth/visibility/age inherited | `tests/material-search.test.mjs`, `tests/mobile-content-routes-contract.test.mjs` |
| Admin/moderation/compliance | `components/admin/*`, `components/safety/*`, `components/compliance/AccessGate.tsx`, `AdminStatisticsPanel.tsx` → `/api/admin/*`, `/api/reports*`, `/api/legal/acceptances`, `/api/users/me/telegram-notifications` | `reports`, report history/appeals, `moderation_audit_log`, legal documents/acceptances, security logs/incidents, suspensions; server `isAdmin`, report ownership and legal state | `tests/security.test.mjs`, `tests/compliance.test.mjs`, `tests/architecture-security.test.mjs`, `tests/production-contract.test.mjs` |
| Localization | `app/i18n/index.tsx`, `messages.ts`, `LocaleSwitcher.tsx` → `Accept-Language`/`X-BookMeet-Locale` headers on `apiFetch`; server `modules/i18n.js` | `users.preferred_locale`, locale-aware legal documents and auth errors; supported `ru`, `kk`, `en` | `tests/i18n.test.mjs`, `tests/production-contract.test.mjs` |
